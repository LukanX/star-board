import AppStatus from "@/components/ui/AppStatus";

export default function EpisodeLoading() {
  return <AppStatus title="Loading episode file." message="Opening the campaign record and visible notes." />;
}