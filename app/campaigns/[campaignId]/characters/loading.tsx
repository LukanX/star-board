import AppStatus from "@/components/ui/AppStatus";

export default function CharactersLoading() {
  return <AppStatus title="Loading character records." message="Assembling the campaign roster." />;
}