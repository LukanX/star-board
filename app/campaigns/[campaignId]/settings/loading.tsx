import AppStatus from "@/components/ui/AppStatus";

export default function SettingsLoading() {
  return <AppStatus title="Loading campaign settings." message="Checking GM access and preparing the model catalog." />;
}