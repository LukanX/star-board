import AppStatus from "@/components/ui/AppStatus";

export default function NotesLoading() {
  return <AppStatus title="Loading campaign memory." message="Gathering visible notes and episode assignments." />;
}